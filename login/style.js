import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    errorMessage: {
        color: '#f03000', 
        textAlign: 'center',
        marginLeft: 'auto', 
        marginRight: 'auto', 
        marginTop: 20
    },
    mainView: { 
        width: '100%', 
        height: '100%'
    }
});

export default styles;